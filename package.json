{
  "name": "@interactivemap/frontend",
  "version": "0.1.0",
  "description": "Front-end of the Interactive Map",
  "repository": "https://github.com/interactivemap/frontend",
  "author": "Amaury Martiny <amaury.martiny@protonmail.com>",
  "license": "GPL-3.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "prebuild": "if test \"$NOW\" = 1 ; then yarn install ; fi",
    "build": "lerna exec yarn build --scope @interactivemap/app",
    "lint": "tslint packages/**/*.tsx?",
    "now-start": "lerna exec yarn now-start --scope @interactivemap/app",
    "start": "lerna exec yarn start --scope @interactivemap/app",
    "test": "echo Skipped for now."
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.13.0",
    "tslint-react": "^3.6.0",
    "typescript": "^2.9.2"
  }
}
